package Day14.exam4;
/*
  interface :
   1. 상수와, 추상 메소드를 멤버로 갖는다. (default, static)
   2. 인스턴스 생성 불가능
   3. 모든 멤버가 public ( public 키워드를 생략하더라도 public이 default)
*/

public interface OnOff {
  void on();
}
